customtkinter
scdl
yt-dlp
mutagen

# Dependencies of the vendored bandcamp-downloader
# (source/vendor_bandcamp_downloader.py) -- it's bundled directly rather than
# pip-installed, so its own deps are listed here instead.
beautifulsoup4
curl_cffi
browser_cookie3
tqdm
